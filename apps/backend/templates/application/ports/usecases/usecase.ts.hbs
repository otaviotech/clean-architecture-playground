import { Either } from 'fp-ts/Either';
import { AsyncCommand } from '@shared/protocols';

export type {{pascalCase name}}UseCaseInput = {
  foo: string;
};

export type {{pascalCase name}}UseCaseOutputBoundary = {
  bar: string;
};

export type {{pascalCase name}}UseCaseOutput = Either<
  Error,
  {{pascalCase name}}UseCaseOutputBoundary
>;

export type I{{pascalCase name}}UseCase = AsyncCommand<
  {{pascalCase name}}UseCaseInput,
  {{pascalCase name}}UseCaseOutput
>;
